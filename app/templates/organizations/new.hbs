<h2>New Organization</h2>
    <div class="input-group">
      <label>Name</label>
      {{input value=model.name}}
    </div>
    <br/>
    <button {{action 'save'}}>Create Organization</button>
{{outlet}}
